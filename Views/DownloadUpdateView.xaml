<Window x:Class="effetopo.Views.UpdateNotificationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:effetopo.Views"
        xmlns:converters="clr-namespace:effetopo.Views.Converters"
        mc:Ignorable="d"
        Title="{Binding CurrentAppName, StringFormat='{}{0} - Update Available'}" Height="380" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="{Binding CurrentAppName}" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text=" " FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="{Binding UpdateInfo.VersionNumber, StringFormat=Version {0} Available!}" 
                      FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
        </StackPanel>
                  
        <TextBlock Grid.Row="1" 
                  Text="{Binding StatusMessage}" 
                  TextWrapping="Wrap" Margin="0,0,0,10"/>

        <!-- Release date -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="Released: "/>
            <TextBlock Text="{Binding UpdateInfo.ReleaseDate, StringFormat=d}" Margin="5,0,0,0"/>
        </StackPanel>
        
        <!-- Release labels -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="(Pre-release)" 
                     Foreground="Orange" 
                     Margin="0,0,10,0"
                     Visibility="{Binding UpdateInfo.IsPrerelease, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            
            <TextBlock Text="(Mandatory Update)" 
                     Foreground="Red" 
                     Visibility="{Binding UpdateInfo.IsMandatory, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>

        <!-- Release notes -->
        <GroupBox Grid.Row="3" Header="Release Notes" Margin="0,5,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding UpdateInfo.ReleaseNotes}" 
                           TextWrapping="Wrap" Margin="5"/>
            </ScrollViewer>
        </GroupBox>

        <!-- Progress bar for download -->
        <ProgressBar Grid.Row="4" Height="20" 
                     Value="{Binding DownloadProgress}" 
                     Visibility="{Binding IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}" 
                     Margin="0,0,0,10"/>
        
        <!-- Auto-run checkbox -->
        <CheckBox Grid.Row="5" Content="Run installer automatically after Revit closes" 
                  IsChecked="{Binding AutoRunAfterClose}" 
                  Margin="0,0,0,10"
                  IsEnabled="{Binding IsDownloading, Converter={StaticResource InverseBooleanConverter}}"/>

        <!-- Buttons -->
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="Download Update" 
                    Command="{Binding DownloadUpdateCommand}" 
                    Padding="10,5" Margin="0,0,10,0"
                    IsEnabled="{Binding IsDownloading, Converter={StaticResource InverseBooleanConverter}}"/>
            
            <Button Grid.Column="2" Content="Remind Me Later" 
                    Command="{Binding DismissCommand}" 
                    Padding="10,5"/>
        </Grid>
    </Grid>
</Window> 