<Window x:Class="effetopo.Views.LicenseWarningView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:effetopo.Views.Converters"
        Title="License Required" Height="280" Width="500" WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <Image Source="/effetopo;component/Resources/Icons/Warning.png" Width="32" Height="32" Margin="0,0,15,0"
                   VerticalAlignment="Center"/>
            <TextBlock Text="License Required" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>
        
        <TextBlock Grid.Row="1" Text="{Binding WarningMessage}" TextWrapping="Wrap" Margin="0,0,0,20"/>
        
        <TextBlock Grid.Row="2" TextWrapping="Wrap" Margin="0,0,0,15">
            <Run Text="If you already have a license key, click"/>
            <Run Text="Activate License" FontWeight="Bold"/>
            <Run Text="to enter it. If you need to purchase a license, please contact your administrator."/>
        </TextBlock>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Activate License" Width="120" Height="30" Margin="0,0,10,0" 
                    Command="{Binding ActivateLicenseCommand}"/>
            <Button Content="{Binding ContinueButtonText}" Width="120" Height="30"
                    Command="{Binding ContinueCommand}" 
                    Visibility="{Binding AllowContinue, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</Window> 